# environment.yml

channels:
  - conda-forge
  - bioconda

dependencies:
  - bioconda::bedtools=2.31.1
  - bioconda::bcftools=1.24
  - bioconda::samtools=1.24
  - conda-forge::perl
  - conda-forge::parallel

# Note perl is required for parallel - consider replacing